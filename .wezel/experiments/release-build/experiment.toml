description = "Measures release-binary size of the wezel CLI"

[step.build-release]
tool = "exec"
cmd = "cargo build --release --workspace"

[step.measure-size]
tool = "filesize"
glob = "target/release/wezel"
summary.wezel-binary-size = { measurement = "target/release/wezel", bisect = true }
